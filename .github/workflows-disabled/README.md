These workflows are intentionally disabled on `punch-bot/core`.

GitHub Actions only runs YAML under `.github/workflows/`. Files here are kept for reference and can be moved back when this fork should run CI, release, or contributor automation again.
